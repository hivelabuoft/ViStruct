how to setup:

cd ../server
python -m venv venv
source venv/bin/activate   # On Windows: venv\Scripts\activate
pip install fastapi uvicorn python-multipart tobii-research opencv-python numpy

for mac: python3.10 -m pip install fastapi uvicorn python-multipart opencv-python numpy tobii-research

cd ../client
npm install axios

make .env.local file under the client folder

put in: 
NEXT_PUBLIC_API_URL=http://localhost:8000




how to run:

cd in server:

python path  -m venv venv

.\venv\Scripts\activate

uvicorn main:app --reload

uvicorn main:app --reload --log-level debug

in another terminal, cd in client:

npm run dev



requirement:

pip version 25.0

node v >=20.00

python 3.10 must


python3.10 -m venv venv

source venv/bin/activate

deployed:

server: https://server-silent-dew-747-fast-api.fly.dev
client: 



